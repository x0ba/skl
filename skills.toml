# Managed by `skl use` / `skl unuse` / `skl use --all`. Names only — resolve from this machine's library.

[targets]
canonical = ["agents"]
extra = ["claude-code"]

[[skills]]
name = "greploop"
source = "library"
mode = "symlink"
